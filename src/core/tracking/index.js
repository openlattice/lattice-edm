/*
 * @flow
 */

import GoogleAnalyticsEvents, { GOOGLE_TRACKING_ID } from './GoogleAnalyticsEvents';
import { trackingMiddleware } from './TrackingMiddleware';

export {
  GOOGLE_TRACKING_ID,
  GoogleAnalyticsEvents,
  trackingMiddleware,
};
